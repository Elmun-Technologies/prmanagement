[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_OPTIONS = "--max-old-space-size=4096"
  NODE_VERSION = "20"

# React Router uchun — barcha URL -> index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
